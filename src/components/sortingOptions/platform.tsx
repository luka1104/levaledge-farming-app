import React from 'react'
import {
    Text,
    Flex,
    Center,
    Box,
    Button,
} from '@chakra-ui/react';

const Platform = () => {
    return (
        <>
            <Flex>
                <Text>Platform:</Text>
                <Button>All</Button>
                <Button>All</Button>
                <Button>All</Button>
            </Flex>
        </>
    )
}

export default Platform
